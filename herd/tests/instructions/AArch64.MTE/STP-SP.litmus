AArch64 STP-SP
Variant=memtag,sync

(* A non-writeback STP using SP as its base is not tag-checked. *)

{
  uint32_t x[2]={0,0};
  [tag(x)]=:green;
  0:SP=x:red;
  0:X0=1;
  0:X1=2;
}
P0;
  STP W0,W1,[SP];
forall(x={1,2} /\ ~fault(P0,x,TagCheck))
