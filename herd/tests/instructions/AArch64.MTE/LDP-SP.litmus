AArch64 LDP-SP
Variant=memtag,sync

(* A non-writeback LDP using SP as its base is not tag-checked. *)

{
  uint32_t x[2]={1,2};
  [tag(x)]=:green;
  0:SP=x:red;
}
P0;
  LDP W0,W1,[SP];
forall(0:X0=1 /\ 0:X1=2 /\ ~fault(P0,x,TagCheck))
