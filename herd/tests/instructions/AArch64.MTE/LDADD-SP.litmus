AArch64 LDADD-SP
Variant=memtag,sync

(* No-writeback atomic add through SP is not tag-checked. *)
{
  uint32_t x=1;
  [tag(x)]=:green;
  0:SP=x:red;
  0:X0=2;
}
P0;
  LDADD W0,W1,[SP];
forall(x=3 /\ 0:X1=1 /\ ~fault(P0,x,TagCheck))
