AArch64 STR-SP-regoffset
Variant=memtag,sync

(* Register-offset STR is tag-checked even when Rn is SP. *)
{
  [x]=0;
  [tag(x)]=:green;
  0:SP=x:red;
  0:X0=1;
  0:X2=0;
}
P0;
  STR W0,[SP,X2];
forall(fault(P0,x,TagCheck))
