AArch64 STRB-SP
Variant=memtag,sync

(* Byte immediate STR is tag-checked even when Rn is SP. *)
{
  [x]=0;
  [tag(x)]=:green;
  0:SP=x:red;
  0:X0=1;
}
P0;
  STRB W0,[SP];
forall(fault(P0,x,TagCheck))
