AArch64 CAS-SP
Variant=memtag,sync

(* No-writeback compare-and-swap through SP is not tag-checked. *)
{
  uint32_t x=1;
  [tag(x)]=:green;
  0:SP=x:red;
  0:X0=1;
  0:X1=2;
}
P0;
  CAS W0,W1,[SP];
forall(x=2 /\ 0:X0=1 /\ ~fault(P0,x,TagCheck))
