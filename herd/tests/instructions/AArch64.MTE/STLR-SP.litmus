AArch64 STLR-SP
Variant=memtag,sync

(* No-writeback release store through SP is not tag-checked. *)
{
  uint32_t x=0;
  [tag(x)]=:green;
  0:SP=x:red;
  0:X0=1;
}
P0;
  STLR W0,[SP];
forall(x=1 /\ ~fault(P0,x,TagCheck))
