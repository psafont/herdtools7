AArch64 LDAR-SP
Variant=memtag,sync

(* No-writeback acquire load through SP is not tag-checked. *)
{
  uint32_t x=1;
  [tag(x)]=:green;
  0:SP=x:red;
}
P0;
  LDAR W0,[SP];
forall(0:X0=1 /\ ~fault(P0,x,TagCheck))
